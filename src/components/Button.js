import React from 'react';
import { Text, TouchableOpacity, View } from 'react-native';

const Button = () => {
	return (
		<TouchableOpacity>
			<Text>Click Me!</Text>
		</TouchableOpacity>
		);
};

export default Button;